# Backend gateway origin (no trailing slash). Falls back to https://api.gonkascan.com
# at runtime if unset — keep that in mind when promoting between environments.
API_BASE=https://api.gonkascan.com
